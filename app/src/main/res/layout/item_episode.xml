<?xml version="1.0" encoding="utf-8"?>
<!-- 播客单集条目（三行排版参照 AntennaPod 的 feeditemlist_item）：
         第一行  年月日·大小       第二行 标题       第三行 时长·下载状态
     右侧是圆形动作按钮（下载 / 删除），参照 AntennaPod 的 secondary_action：
     48dp 容器 + 24dp 图标 + 40dp 进度环。
     下载中时环上显示真实进度（批量下载时也能逐条看到进度），其余状态只显示图标。
     长按进入多选时，左侧勾选框显示、右侧按钮隐藏（由多选操作栏统一操作）。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="12dp"
    android:paddingVertical="10dp"
    android:background="@drawable/bg_item_audio">

    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/cbSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:clickable="false"
        android:focusable="false"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/ivCover"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_playlist_empty"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <!-- 第一行：发布日期 · 文件大小 -->
        <TextView
            android:id="@+id/tvMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 第二行：单集标题 -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:maxLines="2"
            android:ellipsize="end" />

        <!-- 第三行：时长 · 是否已下载 -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/btnDownload"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="false">

        <ImageView
            android:id="@+id/ivDownloadIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@string/download"
            android:src="@drawable/ic_download"
            app:tint="@color/text_primary" />

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/downloadProgress"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:visibility="gone"
            app:indicatorColor="@color/purple_primary"
            app:indicatorDirectionCircular="clockwise"
            app:indicatorSize="36dp"
            app:trackColor="@color/progress_track"
            app:trackThickness="3dp" />

    </FrameLayout>

</LinearLayout>
